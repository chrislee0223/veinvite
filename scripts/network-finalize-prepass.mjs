import { readFile, writeFile } from 'node:fs/promises';

const networkPath = 'src/components/AppNetwork.tsx';
const testPath = 'tests/network-single-runtime.test.mjs';
let source = await readFile(networkPath, 'utf8');
let tests = await readFile(testPath, 'utf8');

function replaceOnce(label, from, to) {
  const count = source.split(from).length - 1;
  if (count !== 1) throw new Error(`${label}: expected exactly one match, found ${count}`);
  source = source.replace(from, to);
}

replaceOnce('workspace helper imports', `  EMPTY_NETWORK_WORKSPACE_STORE,\n  addWorkspaceGroup,\n  cloneNetworkFocusWorkspace,\n  parseNetworkWorkspaceStore,\n  removeWorkspaceGroup,\n  serializeNetworkWorkspaceStore,\n  withFocusWorkspace,\n  withGroupPosition,\n  withNodePosition,\n  workspaceForFocus,`, `  EMPTY_NETWORK_WORKSPACE_STORE,\n  addWorkspaceGroup,\n  cloneNetworkFocusWorkspace,\n  groupContainingWallet,\n  moveWorkspaceMemberToGroup,\n  parseNetworkWorkspaceStore,\n  removeWorkspaceGroup,\n  removeWorkspaceMemberFromGroup,\n  serializeNetworkWorkspaceStore,\n  withoutNodePositions,\n  withFocusWorkspace,\n  withGroupPosition,\n  withNodePosition,\n  withWorkspaceGroupCollapsed,\n  workspaceForFocus,`);

replaceOnce('final zoom range', `const MIN_SCALE = 0.68;\nconst MAX_SCALE = 1.55;`, `const MIN_SCALE = 0.32;\nconst MAX_SCALE = 2.5;`);

replaceOnce('groups panel state', `  const [editingLayout, setEditingLayout] = useState(false);\n  const [draftWorkspace, setDraftWorkspace] = useState<NetworkFocusWorkspace | null>(null);`, `  const [editingLayout, setEditingLayout] = useState(false);\n  const [groupsOpen, setGroupsOpen] = useState(false);\n  const [draftWorkspace, setDraftWorkspace] = useState<NetworkFocusWorkspace | null>(null);`);

replaceOnce('grouped member visibility', `  const stagedGroupMembers = useMemo(() => {\n    const keys = new Set<string>();\n    activeWorkspace.groups.forEach((group) => group.members.forEach((member) => keys.add(keyWallet(member))));\n    groupDraft?.members.forEach((member) => keys.add(keyWallet(member)));\n    return keys;\n  }, [activeWorkspace.groups, groupDraft]);\n\n  const visibleChildren = useMemo(\n    () => positionedChildren.filter((child) => !stagedGroupMembers.has(keyWallet(child.wallet))),\n    [positionedChildren, stagedGroupMembers],\n  );`, `  const groupedWalletKeys = useMemo(() => {\n    const keys = new Set<string>();\n    activeWorkspace.groups.forEach((group) => group.members.forEach((member) => keys.add(keyWallet(member))));\n    groupDraft?.members.forEach((member) => keys.add(keyWallet(member)));\n    return keys;\n  }, [activeWorkspace.groups, groupDraft]);\n\n  const visibleChildren = useMemo(\n    () => positionedChildren.filter((child) => !groupedWalletKeys.has(keyWallet(child.wallet))),\n    [positionedChildren, groupedWalletKeys],\n  );`);

replaceOnce('expanded group members', `  const visibleGroups = useMemo(\n    () => activeWorkspace.groups.filter((group) => group.members.some((member) => visibleWalletKeys.has(keyWallet(member)))),\n    [activeWorkspace.groups, visibleWalletKeys],\n  );\n\n  const childByWallet = useMemo(() => {`, `  const visibleGroups = useMemo(\n    () => activeWorkspace.groups.filter((group) => group.members.some((member) => visibleWalletKeys.has(keyWallet(member)))),\n    [activeWorkspace.groups, visibleWalletKeys],\n  );\n\n  const positionedByWallet = useMemo(() => {\n    const map = new Map<string, PositionedChild>();\n    positionedChildren.forEach((child) => map.set(keyWallet(child.wallet), child));\n    return map;\n  }, [positionedChildren]);\n\n  const expandedGroupMembers = useMemo(() => (\n    visibleGroups.flatMap((group) => (\n      group.collapsed === false\n        ? group.members.flatMap((member) => {\n            const child = positionedByWallet.get(keyWallet(member));\n            return child ? [{ group, child }] : [];\n          })\n        : []\n    ))\n  ), [visibleGroups, positionedByWallet]);\n\n  const childByWallet = useMemo(() => {`);

replaceOnce('wallet reset groups panel', `    setSelectedGroupId(null);\n    setEditingLayout(false);\n    setDraftWorkspace(null);`, `    setSelectedGroupId(null);\n    setEditingLayout(false);\n    setGroupsOpen(false);\n    setDraftWorkspace(null);`);

replaceOnce('focus reset groups panel', `  useEffect(() => {\n    setEditingLayout(false);\n    setDraftWorkspace(null);\n    setGroupDraft(null);`, `  useEffect(() => {\n    setEditingLayout(false);\n    setGroupsOpen(false);\n    setDraftWorkspace(null);\n    setGroupDraft(null);`);

replaceOnce('layout edit closes groups', `  const beginLayoutEdit = useCallback(() => {\n    if (!currentFocusKey) return;\n    setDraftWorkspace(cloneNetworkFocusWorkspace(workspaceForFocus(workspaceStore, currentFocusKey)));\n    setEditingLayout(true);`, `  const beginLayoutEdit = useCallback(() => {\n    if (!currentFocusKey) return;\n    setDraftWorkspace(cloneNetworkFocusWorkspace(workspaceForFocus(workspaceStore, currentFocusKey)));\n    setEditingLayout(true);\n    setGroupsOpen(false);`);

replaceOnce('layout cancel closes groups', `    setSelectedGroupId(null);\n    setEditingLayout(false);\n  }, []);\n\n  const saveLayoutEdit`, `    setSelectedGroupId(null);\n    setEditingLayout(false);\n    setGroupsOpen(false);\n  }, []);\n\n  const saveLayoutEdit`);

replaceOnce('layout save closes groups', `    setSelectedGroupId(null);\n    setEditingLayout(false);\n    setWorkspaceNotice(w.layoutSaved);`, `    setSelectedGroupId(null);\n    setEditingLayout(false);\n    setGroupsOpen(false);\n    setWorkspaceNotice(w.layoutSaved);`);

replaceOnce('group helpers before builder', `  const openGroupBuilder = useCallback(() => {\n    if (!editingLayout) return;\n    setSelectedGroupId(null);\n    setGroupDraft({ id: newGroupId(), label: '', members: [] });\n  }, [editingLayout]);`, `  const commitFocusWorkspace = useCallback((nextWorkspace: NetworkFocusWorkspace) => {\n    if (!wallet || !currentFocusKey) return;\n    setWorkspaceStore((current) => {\n      const nextStore = withFocusWorkspace(current, currentFocusKey, nextWorkspace);\n      try {\n        window.localStorage.setItem(workspaceStorageKey(wallet), serializeNetworkWorkspaceStore(nextStore));\n      } catch {\n        // In-memory state remains valid when local storage is unavailable.\n      }\n      return nextStore;\n    });\n  }, [wallet, currentFocusKey]);\n\n  const toggleGroupCollapsed = useCallback((groupId: string) => {\n    const current = editingLayout && draftWorkspace ? draftWorkspace : committedWorkspace;\n    const group = current.groups.find((candidate) => candidate.id === groupId);\n    if (!group) return;\n    const next = withWorkspaceGroupCollapsed(current, groupId, group.collapsed === false);\n    if (editingLayout) setDraftWorkspace(next);\n    else commitFocusWorkspace(next);\n  }, [editingLayout, draftWorkspace, committedWorkspace, commitFocusWorkspace]);\n\n  const resetLayoutEdit = useCallback(() => {\n    setDraftWorkspace((current) => {\n      if (!current) return current;\n      const reset = withoutNodePositions(current);\n      const count = reset.groups.length;\n      return {\n        ...reset,\n        groups: reset.groups.map((group, index) => ({\n          ...group,\n          x: FOCUS_X + (index - (count - 1) / 2) * 120,\n          y: FOCUS_Y + 215,\n        })),\n      };\n    });\n    setGroupDraft(null);\n    setSelectedGroupId(null);\n  }, []);\n\n  const beginGroupCreation = useCallback(() => {\n    if (!currentFocusKey) return;\n    if (!editingLayout) {\n      setDraftWorkspace(cloneNetworkFocusWorkspace(workspaceForFocus(workspaceStore, currentFocusKey)));\n      setEditingLayout(true);\n    }\n    setGroupsOpen(false);\n    setSelectedGroupId(null);\n    setGroupDraft({ id: newGroupId(), label: '', members: [] });\n  }, [currentFocusKey, editingLayout, workspaceStore]);\n\n  const openGroupBuilder = useCallback(() => {\n    if (!editingLayout) return;\n    setGroupsOpen(false);\n    setSelectedGroupId(null);\n    setGroupDraft({ id: newGroupId(), label: '', members: [] });\n  }, [editingLayout]);`);

replaceOnce('existing group drop support', `  const finishWorkspaceDrop = useCallback((event: ReactPointerEvent<HTMLDivElement>, drag: WorkspaceDrag) => {\n    if (drag.kind !== 'node' || !groupDraft || event.type !== 'pointerup') return;\n    const drop = groupDropRef.current?.getBoundingClientRect();\n    if (!drop) return;\n    const inside = event.clientX >= drop.left && event.clientX <= drop.right && event.clientY >= drop.top && event.clientY <= drop.bottom;\n    if (!inside || groupDraft.members.includes(drag.key)) return;\n    setGroupingWallet(drag.key);\n    if (groupingTimerRef.current !== null) window.clearTimeout(groupingTimerRef.current);\n    groupingTimerRef.current = window.setTimeout(() => {\n      groupingTimerRef.current = null;\n      setGroupDraft((current) => {\n        if (!current || current.members.includes(drag.key)) return current;\n        return { ...current, members: [...current.members, drag.key] };\n      });\n      setGroupingWallet(null);\n    }, GROUP_DROP_MS);\n  }, [groupDraft]);`, `  const finishWorkspaceDrop = useCallback((event: ReactPointerEvent<HTMLDivElement>, drag: WorkspaceDrag) => {\n    if (drag.kind !== 'node' || event.type !== 'pointerup' || !draftWorkspace) return;\n\n    const targetGroup = document.elementsFromPoint(event.clientX, event.clientY)\n      .map((element) => element.closest<HTMLElement>('[data-workspace-group-id]'))\n      .find((element): element is HTMLElement => Boolean(element));\n    const targetGroupId = targetGroup?.dataset.workspaceGroupId;\n    if (targetGroupId) {\n      setGroupingWallet(drag.key);\n      setDraftWorkspace((current) => current ? moveWorkspaceMemberToGroup(current, drag.key, targetGroupId) : current);\n      if (groupingTimerRef.current !== null) window.clearTimeout(groupingTimerRef.current);\n      groupingTimerRef.current = window.setTimeout(() => {\n        groupingTimerRef.current = null;\n        setGroupingWallet(null);\n      }, GROUP_DROP_MS);\n      return;\n    }\n\n    if (!groupDraft) return;\n    const drop = groupDropRef.current?.getBoundingClientRect();\n    if (!drop) return;\n    const inside = event.clientX >= drop.left && event.clientX <= drop.right && event.clientY >= drop.top && event.clientY <= drop.bottom;\n    if (!inside || groupDraft.members.includes(drag.key)) return;\n    setGroupingWallet(drag.key);\n    if (groupingTimerRef.current !== null) window.clearTimeout(groupingTimerRef.current);\n    groupingTimerRef.current = window.setTimeout(() => {\n      groupingTimerRef.current = null;\n      setGroupDraft((current) => {\n        if (!current || current.members.includes(drag.key)) return current;\n        return { ...current, members: [...current.members, drag.key] };\n      });\n      setGroupingWallet(null);\n    }, GROUP_DROP_MS);\n  }, [draftWorkspace, groupDraft]);`);

replaceOnce('group member and continuation edges', `              {visibleGroups.map((group) => (\n                <path\n                  key={\`group-edge:${group.id}\`}\n                  d={edgePath(FOCUS_X, FOCUS_Y + 42, group.x, group.y - 35)}\n                  className="edge groupEdge"\n                />\n              ))}\n              {Array.from({ length: emptySlotCount }).map((_, index) => {`, `              {visibleGroups.map((group) => (\n                <path\n                  key={\`group-edge:${group.id}\`}\n                  d={edgePath(FOCUS_X, FOCUS_Y + 42, group.x, group.y - 35)}\n                  className="edge groupEdge"\n                />\n              ))}\n              {expandedGroupMembers.map(({ group, child }) => (\n                <path\n                  key={\`group-member-edge:${group.id}:${keyWallet(child.wallet)}\`}\n                  d={edgePath(group.x, group.y + 35, child.x, child.y - 34)}\n                  className="edge groupMemberEdge"\n                />\n              ))}\n              {visibleChildren.filter((child) => child.network > 0).map((child) => (\n                <path\n                  key={\`continuation:${keyWallet(child.wallet)}\`}\n                  d={\`M ${child.x} ${child.y + 35} L ${child.x} ${child.y + 63}\`}\n                  className="continuationEdge"\n                />\n              ))}\n              {expandedGroupMembers.filter(({ child }) => child.network > 0).map(({ group, child }) => (\n                <path\n                  key={\`group-continuation:${group.id}:${keyWallet(child.wallet)}\`}\n                  d={\`M ${child.x} ${child.y + 35} L ${child.x} ${child.y + 63}\`}\n                  className="continuationEdge"\n                />\n              ))}\n              {Array.from({ length: emptySlotCount }).map((_, index) => {`);

replaceOnce('expanded member nodes', `            {visibleGroups.map((group) => {\n              const dragKey = \`group:${group.id}\`;`, `            {expandedGroupMembers.map(({ group, child }) => {\n              const childKey = keyWallet(child.wallet);\n              const dragKey = \`node:${childKey}\`;\n              return (\n                <button\n                  type="button"\n                  key={\`group-member:${group.id}:${childKey}\`}\n                  className={\`personNode childNode groupMemberNode status-${child.status.toLowerCase()}${selectedWallet === childKey ? ' selected' : ''}${editingLayout ? ' draggable' : ''}${draggingWorkspaceKey === dragKey ? ' dragging' : ''}${groupingWallet === childKey ? ' grouping' : ''}\`}\n                  style={{ left: child.x, top: child.y }}\n                  onPointerDown={editingLayout\n                    ? (event) => beginWorkspaceDrag(event, 'node', childKey, { x: child.x, y: child.y })\n                    : undefined}\n                  onClick={() => {\n                    if (suppressClickRef.current || editingLayout) return;\n                    setSelectedGroupId(null);\n                    setSelectedWallet(childKey);\n                  }}\n                  data-no-pan="true"\n                  data-network-wallet={child.wallet}\n                  data-workspace-draggable={editingLayout ? 'true' : undefined}\n                >\n                  <NetworkIdentity address={child.wallet} />\n                  <span className="nodeMeta">\n                    <strong>{shortWallet(child.wallet)}</strong>\n                    <small>{statusLabel(child.status, locale)}</small>\n                  </span>\n                  {pendingFocus === childKey ? <span className="nodeBusy" aria-hidden="true" /> : null}\n                </button>\n              );\n            })}\n\n            {visibleGroups.map((group) => {\n              const dragKey = \`group:${group.id}\`;`);

replaceOnce('group hub interaction', `                  onClick={() => {\n                    if (suppressClickRef.current) return;\n                    setSelectedWallet(null);\n                    setSelectedGroupId(group.id);\n                  }}\n                  data-no-pan="true"\n                  data-workspace-draggable={editingLayout ? 'true' : undefined}`, `                  onClick={() => {\n                    if (suppressClickRef.current) return;\n                    setSelectedWallet(null);\n                    if (editingLayout) setSelectedGroupId(group.id);\n                    else toggleGroupCollapsed(group.id);\n                  }}\n                  data-no-pan="true"\n                  data-workspace-group-id={group.id}\n                  data-workspace-draggable={editingLayout ? 'true' : undefined}`);

replaceOnce('layout controls final', `        <div className="layoutControls" data-no-pan="true">\n          {!editingLayout ? (\n            <button type="button" className="editLayoutButton" onClick={beginLayoutEdit}>✦ {w.editLayout}</button>\n          ) : (\n            <>\n              <button type="button" className="newGroupButton" onClick={openGroupBuilder}>+ {w.newGroup}</button>\n              <button type="button" className="cancelLayoutButton" onClick={cancelLayoutEdit}>{w.cancel}</button>\n              <button type="button" className="saveLayoutButton" onClick={saveLayoutEdit}>{w.save}</button>\n            </>\n          )}\n        </div>`, `        <div className="layoutControls" data-no-pan="true">\n          <button type="button" className="groupsButton" onClick={() => setGroupsOpen((value) => !value)}>{w.groups}</button>\n          {!editingLayout ? (\n            <button type="button" className="editLayoutButton" onClick={beginLayoutEdit}>✦ {w.editLayout}</button>\n          ) : (\n            <>\n              <button type="button" className="newGroupButton" onClick={openGroupBuilder}>+ {w.newGroup}</button>\n              <button type="button" className="resetLayoutButton" onClick={resetLayoutEdit}>{w.reset}</button>\n              <button type="button" className="cancelLayoutButton" onClick={cancelLayoutEdit}>{w.cancel}</button>\n              <button type="button" className="saveLayoutButton" onClick={saveLayoutEdit}>{w.done}</button>\n            </>\n          )}\n        </div>\n\n        {groupsOpen ? (\n          <aside className="groupsPanel" data-no-pan="true">\n            <div className="groupsPanelHead">\n              <div><strong>{w.myGroups}</strong><span>{activeWorkspace.groups.length}</span></div>\n              <button type="button" onClick={() => setGroupsOpen(false)} aria-label={c.close}>×</button>\n            </div>\n            {activeWorkspace.groups.length ? (\n              <div className="groupsPanelList">\n                {activeWorkspace.groups.map((group) => (\n                  <button\n                    type="button"\n                    key={group.id}\n                    onClick={() => { setSelectedWallet(null); setSelectedGroupId(group.id); }}\n                  >\n                    <strong>{group.label || w.group}</strong>\n                    <span>{group.members.length} {w.members} · {group.collapsed === false ? w.collapseGroup : w.expandGroup}</span>\n                  </button>\n                ))}\n              </div>\n            ) : <p className="groupsEmpty">{w.noGroups}</p>}\n            <button type="button" className="groupsCreateButton" onClick={beginGroupCreation}>+ {w.newGroup}</button>\n          </aside>\n        ) : null}`);

replaceOnce('group card controls', `            <div className="groupMemberList">\n              {selectedGroup.members.map((member) => <span key={member} title={member}>{shortWallet(member)}</span>)}\n            </div>\n            {editingLayout ? (\n              <button\n                type="button"\n                className="ungroupButton"\n                onClick={() => {\n                  setDraftWorkspace((current) => current ? removeWorkspaceGroup(current, selectedGroup.id) : current);\n                  setSelectedGroupId(null);\n                }}\n              >{w.ungroup}</button>\n            ) : null}`, `            <div className="groupMemberList">\n              {selectedGroup.members.map((member) => editingLayout ? (\n                <button\n                  type="button"\n                  key={member}\n                  title={member}\n                  onClick={() => setDraftWorkspace((current) => current ? removeWorkspaceMemberFromGroup(current, member) : current)}\n                >{shortWallet(member)} ×</button>\n              ) : <span key={member} title={member}>{shortWallet(member)}</span>)}\n            </div>\n            <button type="button" className="groupToggleButton" onClick={() => toggleGroupCollapsed(selectedGroup.id)}>\n              {selectedGroup.collapsed === false ? w.collapseGroup : w.expandGroup}\n            </button>\n            {editingLayout ? (\n              <button\n                type="button"\n                className="ungroupButton"\n                onClick={() => {\n                  setDraftWorkspace((current) => current ? removeWorkspaceGroup(current, selectedGroup.id) : current);\n                  setSelectedGroupId(null);\n                }}\n              >{w.ungroup}</button>\n            ) : null}`);

replaceOnce('group edge css', `.edge.rewarded{stroke:rgba(232,183,62,.46)}.edge.groupEdge{stroke:rgba(224,178,65,.42);stroke-width:1.5}.slotEdge`, `.edge.rewarded{stroke:rgba(232,183,62,.46)}.edge.groupEdge{stroke:rgba(224,178,65,.42);stroke-width:1.5}.edge.groupMemberEdge{stroke:rgba(195,158,72,.35)}.continuationEdge{fill:none;stroke:rgba(176,145,73,.26);stroke-width:1.2;stroke-linecap:round;vector-effect:non-scaling-stroke}.slotEdge`);

replaceOnce('groups panel css', `.layoutControls .cancelLayoutButton{color:#8d877e}\n        .groupBuilder`, `.layoutControls .cancelLayoutButton{color:#8d877e}.layoutControls .resetLayoutButton,.layoutControls .groupsButton{color:#b0a47f}\n        .groupsPanel{position:absolute;z-index:82;left:10px;top:50px;width:min(235px,calc(100% - 20px));box-sizing:border-box;padding:11px;border:1px solid rgba(244,183,40,.2);border-radius:15px;background:rgba(14,14,12,.985);box-shadow:0 18px 40px rgba(0,0,0,.42);cursor:default}.groupsPanelHead{display:flex;align-items:center;justify-content:space-between}.groupsPanelHead>div{display:flex;align-items:center;gap:6px}.groupsPanelHead strong{color:#e5dfd3;font-size:.62rem}.groupsPanelHead span{color:#8e805a;font-size:.5rem}.groupsPanelHead>button{width:27px;height:27px;border:0;background:transparent;color:#817c73;font-size:.95rem;cursor:pointer}.groupsPanelList{display:grid;gap:5px;margin-top:7px}.groupsPanelList button{padding:8px;border:1px solid rgba(255,255,255,.06);border-radius:9px;background:rgba(255,255,255,.025);color:#d2cab9;text-align:left;font:inherit;cursor:pointer}.groupsPanelList strong,.groupsPanelList span{display:block}.groupsPanelList strong{font-size:.54rem}.groupsPanelList span{margin-top:2px;color:#736d62;font-size:.46rem}.groupsEmpty{margin:10px 0;color:#716b62;font-size:.52rem;text-align:center}.groupsCreateButton{width:100%;min-height:32px;margin-top:8px;border:1px solid rgba(244,183,40,.22);border-radius:9px;background:rgba(244,183,40,.06);color:#c9a64d;font:inherit;font-size:.52rem;font-weight:900;cursor:pointer}\n        .groupBuilder`);

replaceOnce('group card button css', `.groupMemberList span{padding:4px 6px;border-radius:7px;background:rgba(255,255,255,.025);color:#817b71;font-size:.47rem}`, `.groupMemberList span,.groupMemberList button{padding:4px 6px;border:0;border-radius:7px;background:rgba(255,255,255,.025);color:#817b71;font:inherit;font-size:.47rem}.groupMemberList button{cursor:pointer}.groupToggleButton,.ungroupButton`);

if (!tests.includes('final zoom range spans 32% to 250%')) {
  tests += `\n\ntest('final zoom range spans 32% to 250% without changing node coordinates', () => {\n  assert.match(networkSource, /const MIN_SCALE = 0\\.32/);\n  assert.match(networkSource, /const MAX_SCALE = 2\\.5/);\n  assert.doesNotMatch(workspaceSource, /scale|camera/);\n});\n\ntest('Groups mode supports collapsed hubs, expanded member edges, transfer, removal, and no root-member duplicate edge', () => {\n  assert.match(networkSource, /const \\[groupsOpen, setGroupsOpen\\] = useState\\(false\\)/);\n  assert.match(networkSource, /expandedGroupMembers/);\n  assert.match(networkSource, /groupMemberEdge/);\n  assert.match(networkSource, /data-workspace-group-id=\\{group\\.id\\}/);\n  assert.match(networkSource, /moveWorkspaceMemberToGroup/);\n  assert.match(networkSource, /removeWorkspaceMemberFromGroup/);\n  assert.match(networkSource, /toggleGroupCollapsed/);\n  assert.match(workspaceSource, /if \\(!target \\|\\| target\\.members\\.some/);\n});\n\ntest('Edit layout exposes Done and Reset while group creation remains draft-first', () => {\n  assert.match(networkSource, /onClick=\\{resetLayoutEdit\\}>\\{w\\.reset\\}/);\n  assert.match(networkSource, /onClick=\\{saveLayoutEdit\\}>\\{w\\.done\\}/);\n  assert.match(networkSource, /withoutNodePositions/);\n  assert.match(networkSource, /groupDraft\\.members\\.length < 2/);\n});\n\ntest('deeper descendants use a continuation line instead of +N badges', () => {\n  assert.match(networkSource, /className="continuationEdge"/);\n  assert.doesNotMatch(networkSource, /hidden descendants|deeper network|\\+N/);\n});\n`;
}

await writeFile(networkPath, source);
await writeFile(testPath, tests);
