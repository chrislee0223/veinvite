import { readFile, writeFile } from 'node:fs/promises';

const networkPath = 'src/components/AppNetwork.tsx';
const testPath = 'tests/network-single-runtime.test.mjs';
let source = await readFile(networkPath, 'utf8');
let tests = await readFile(testPath, 'utf8');

function replaceOnce(label, from, to) {
  const count = source.split(from).length - 1;
  if (count !== 1) throw new Error(`${label}: expected exactly one match, found ${count}`);
  source = source.replace(from, to);
}

replaceOnce('hold type', `type GroupDraft = {\n  id: string;\n  label: string;\n  members: string[];\n};`, `type HoldDragState = {\n  pointerId: number;\n  key: string;\n  startScreen: Point;\n  startNode: Point;\n  offset: Point;\n  armed: boolean;\n  moved: boolean;\n  lastPoint: Point;\n};\n\ntype GroupDraft = {\n  id: string;\n  label: string;\n  members: string[];\n};`);

replaceOnce('gesture constants', `const GROUP_DROP_MS = 160;\nconst SESSION_PREFIX`, `const GROUP_DROP_MS = 160;\nconst HOLD_TO_MOVE_MS = 500;\nconst HOLD_CANCEL_DISTANCE = 8;\nconst PINCH_ENTER_OVERSHOOT = 1.08;\nconst WHEEL_ENTER_DISTANCE = 120;\nconst SESSION_PREFIX`);

replaceOnce('gesture refs', `  const pinchReturnIntentRef = useRef(false);\n  const wheelReturnDistanceRef = useRef(0);`, `  const pinchReturnIntentRef = useRef(false);\n  const pinchEnterIntentRef = useRef<string | null>(null);\n  const wheelReturnDistanceRef = useRef(0);\n  const wheelEnterDistanceRef = useRef(0);\n  const holdDragRef = useRef<HoldDragState | null>(null);\n  const holdTimerRef = useRef<number | null>(null);`);

replaceOnce('workspace timer cleanup', `    if (noticeTimerRef.current !== null) {\n      window.clearTimeout(noticeTimerRef.current);\n      noticeTimerRef.current = null;\n    }\n  }, []);`, `    if (noticeTimerRef.current !== null) {\n      window.clearTimeout(noticeTimerRef.current);\n      noticeTimerRef.current = null;\n    }\n    if (holdTimerRef.current !== null) {\n      window.clearTimeout(holdTimerRef.current);\n      holdTimerRef.current = null;\n    }\n    holdDragRef.current = null;\n    pinchEnterIntentRef.current = null;\n  }, []);`);

replaceOnce('ancestor camera restoration', `      if (direction === 'back') {\n        const exactParentView = returnViewByChildRef.current.get(current);\n        setView(\n          exactParentView ??\n          viewByFocusRef.current.get(target) ??\n          centeredView(stageSize, 1),\n        );\n      } else {`, `      if (direction === 'back') {\n        const immediateParent = currentData.breadcrumb[currentData.breadcrumb.length - 2];\n        const isImmediateParent = Boolean(immediateParent && keyWallet(immediateParent) === target);\n        const exactParentView = isImmediateParent ? returnViewByChildRef.current.get(current) : undefined;\n        setView(\n          exactParentView ??\n          viewByFocusRef.current.get(target) ??\n          centeredView(stageSize, 1),\n        );\n      } else {`);

replaceOnce('fit and root controls', `  const centerNetwork = useCallback(() => {\n    if (stageSize.width <= 0 || stageSize.height <= 0) return;\n    setCameraTransition(true);\n    setView(centeredView(stageSize, 1));\n    window.setTimeout(() => setCameraTransition(false), 240);\n  }, [stageSize]);`, `  const fitNetwork = useCallback(() => {\n    if (stageSize.width <= 0 || stageSize.height <= 0) return;\n    const points: Point[] = [\n      { x: FOCUS_X, y: FOCUS_Y },\n      ...visibleChildren.map((child) => ({ x: child.x, y: child.y })),\n      ...visibleGroups.map((group) => ({ x: group.x, y: group.y })),\n    ];\n    if (emptySlotCount > 0) points.push({ x: FOCUS_X - 95, y: FOCUS_Y + 184 });\n    if (emptySlotCount > 1) points.push({ x: FOCUS_X + 95, y: FOCUS_Y + 184 });\n    const xs = points.map((point) => point.x);\n    const ys = points.map((point) => point.y);\n    const minX = Math.min(...xs);\n    const maxX = Math.max(...xs);\n    const minY = Math.min(...ys);\n    const maxY = Math.max(...ys);\n    const contentWidth = Math.max(260, maxX - minX + 230);\n    const contentHeight = Math.max(260, maxY - minY + 220);\n    const scale = clamp(\n      Math.min((stageSize.width - 28) / contentWidth, (stageSize.height - 28) / contentHeight),\n      MIN_SCALE,\n      Math.min(MAX_SCALE, 1.25),\n    );\n    const centerX = (minX + maxX) / 2;\n    const centerY = (minY + maxY) / 2;\n    setCameraTransition(true);\n    setView({\n      x: stageSize.width / 2 - centerX * scale,\n      y: stageSize.height / 2 - centerY * scale,\n      scale,\n    });\n    window.setTimeout(() => setCameraTransition(false), 240);\n  }, [stageSize, visibleChildren, visibleGroups, emptySlotCount]);\n\n  const returnToRoot = useCallback(() => {\n    if (!currentData || pendingFocus || editingLayout) return;\n    if (keyWallet(currentData.focusWallet) === keyWallet(currentData.rootWallet)) {\n      fitNetwork();\n      return;\n    }\n    void moveToFocus(currentData.rootWallet, 'back');\n  }, [currentData, pendingFocus, editingLayout, fitNetwork, moveToFocus]);`);

replaceOnce('hold starter insertion', `  const finishWorkspaceDrop = useCallback((event: ReactPointerEvent<HTMLDivElement>, drag: WorkspaceDrag) => {`, `  const beginHoldNode = useCallback((\n    event: ReactPointerEvent<HTMLButtonElement>,\n    key: string,\n    point: Point,\n  ) => {\n    if (editingLayout || groupsOpen || pendingFocus || groupContainingWallet(committedWorkspace, key)) return;\n    const stage = stageRef.current;\n    if (!stage) return;\n    if (holdTimerRef.current !== null) window.clearTimeout(holdTimerRef.current);\n    const pointerId = event.pointerId;\n    const rect = stage.getBoundingClientRect();\n    const worldPoint = {\n      x: (event.clientX - rect.left - view.x) / view.scale,\n      y: (event.clientY - rect.top - view.y) / view.scale,\n    };\n    holdDragRef.current = {\n      pointerId,\n      key,\n      startScreen: { x: event.clientX, y: event.clientY },\n      startNode: { ...point },\n      offset: { x: worldPoint.x - point.x, y: worldPoint.y - point.y },\n      armed: false,\n      moved: false,\n      lastPoint: { ...point },\n    };\n    holdTimerRef.current = window.setTimeout(() => {\n      holdTimerRef.current = null;\n      const hold = holdDragRef.current;\n      if (!hold || hold.pointerId !== pointerId || pointersRef.current.size !== 1) return;\n      hold.armed = true;\n      suppressClickRef.current = true;\n      setDraggingWorkspaceKey(`node:${key}`);\n    }, HOLD_TO_MOVE_MS);\n  }, [editingLayout, groupsOpen, pendingFocus, committedWorkspace, view]);\n\n  const finishWorkspaceDrop = useCallback((event: ReactPointerEvent<HTMLDivElement>, drag: WorkspaceDrag) => {`);

replaceOnce('second pointer cancels hold', `    if (pointersRef.current.size === 2) {\n      workspaceDragRef.current = null;\n      setDraggingWorkspaceKey(null);`, `    if (pointersRef.current.size === 2) {\n      if (holdTimerRef.current !== null) {\n        window.clearTimeout(holdTimerRef.current);\n        holdTimerRef.current = null;\n      }\n      const hold = holdDragRef.current;\n      if (hold?.moved && currentFocusKey) {\n        setWorkspaceStore((current) => withFocusWorkspace(\n          current,\n          currentFocusKey,\n          withNodePosition(workspaceForFocus(current, currentFocusKey), hold.key, hold.startNode),\n        ));\n      }\n      holdDragRef.current = null;\n      pinchEnterIntentRef.current = null;\n      workspaceDragRef.current = null;\n      setDraggingWorkspaceKey(null);`);

replaceOnce('pinch start reset', `      pinchRef.current = {\n        startDistance: Math.max(1, distance(a, b)),`, `      pinchEnterIntentRef.current = null;\n      pinchRef.current = {\n        startDistance: Math.max(1, distance(a, b)),`);

replaceOnce('hold move insertion', `    const workspaceDrag = workspaceDragRef.current;\n    if (`, `    const hold = holdDragRef.current;\n    if (hold && hold.pointerId === event.pointerId && pointersRef.current.size === 1 && !editingLayout) {\n      const screenPoint = { x: event.clientX, y: event.clientY };\n      const travel = distance(hold.startScreen, screenPoint);\n      if (!hold.armed) {\n        if (travel > HOLD_CANCEL_DISTANCE) {\n          if (holdTimerRef.current !== null) window.clearTimeout(holdTimerRef.current);\n          holdTimerRef.current = null;\n          holdDragRef.current = null;\n          suppressClickRef.current = true;\n        }\n        return;\n      }\n      const rect = stageRef.current?.getBoundingClientRect();\n      if (!rect || !currentFocusKey) return;\n      const nextPoint = {\n        x: clamp((event.clientX - rect.left - view.x) / view.scale - hold.offset.x, 90, WORLD_W - 90),\n        y: clamp((event.clientY - rect.top - view.y) / view.scale - hold.offset.y, 90, WORLD_H - 90),\n      };\n      if (distance(hold.lastPoint, nextPoint) > 0.5) {\n        hold.moved = true;\n        hold.lastPoint = nextPoint;\n        setWorkspaceStore((current) => withFocusWorkspace(\n          current,\n          currentFocusKey,\n          withNodePosition(workspaceForFocus(current, currentFocusKey), hold.key, nextPoint),\n        ));\n      }\n      suppressClickRef.current = true;\n      return;\n    }\n\n    const workspaceDrag = workspaceDragRef.current;\n    if (`);

replaceOnce('pinch enter intent', `      if (!editingLayout && rawScale < MIN_SCALE * 0.88 && currentData && currentData.breadcrumb.length > 1) {\n        pinchReturnIntentRef.current = true;\n      }\n      suppressClickRef.current = true;`, `      if (!editingLayout && rawScale < MIN_SCALE * 0.88 && currentData && currentData.breadcrumb.length > 1) {\n        pinchReturnIntentRef.current = true;\n      }\n      if (!editingLayout && rawScale > MAX_SCALE * PINCH_ENTER_OVERSHOOT) {\n        const hit = document.elementFromPoint(center.x, center.y)?.closest<HTMLElement>('[data-network-wallet]');\n        const candidate = hit?.dataset.networkWallet;\n        if (candidate && childByWallet.has(keyWallet(candidate))) pinchEnterIntentRef.current = candidate;\n      }\n      suppressClickRef.current = true;`);

replaceOnce('hold finish insertion', `  const onPointerEndCapture = (event: ReactPointerEvent<HTMLDivElement>) => {\n    const workspaceDrag = workspaceDragRef.current;`, `  const onPointerEndCapture = (event: ReactPointerEvent<HTMLDivElement>) => {\n    const hold = holdDragRef.current;\n    if (hold && hold.pointerId === event.pointerId) {\n      if (holdTimerRef.current !== null) window.clearTimeout(holdTimerRef.current);\n      holdTimerRef.current = null;\n      if (hold.moved && currentFocusKey) {\n        if (event.type === 'pointercancel') {\n          setWorkspaceStore((current) => withFocusWorkspace(\n            current,\n            currentFocusKey,\n            withNodePosition(workspaceForFocus(current, currentFocusKey), hold.key, hold.startNode),\n          ));\n        } else {\n          setWorkspaceStore((current) => {\n            const next = withFocusWorkspace(\n              current,\n              currentFocusKey,\n              withNodePosition(workspaceForFocus(current, currentFocusKey), hold.key, hold.lastPoint),\n            );\n            if (wallet) {\n              try { window.localStorage.setItem(workspaceStorageKey(wallet), serializeNetworkWorkspaceStore(next)); } catch { /* best effort */ }\n            }\n            return next;\n          });\n        }\n      }\n      if (hold.armed) suppressClickRef.current = true;\n      holdDragRef.current = null;\n      setDraggingWorkspaceKey(null);\n    }\n\n    const workspaceDrag = workspaceDragRef.current;`);

replaceOnce('pinch completion routing', `    if (pointersRef.current.size === 1) {\n      const [remainingId, remainingPoint] = Array.from(pointersRef.current.entries())[0];\n      panPointerRef.current = { id: remainingId, point: remainingPoint, allowed: false };\n      pinchRef.current = null;\n      if (pinchReturnIntentRef.current) {\n        pinchReturnIntentRef.current = false;\n        returnToParent();\n      }\n      return;\n    }\n    if (pointersRef.current.size === 0) {\n      panPointerRef.current = null;\n      pinchRef.current = null;\n      if (pinchReturnIntentRef.current) {\n        pinchReturnIntentRef.current = false;\n        returnToParent();\n      }`, `    if (pointersRef.current.size === 1) {\n      const [remainingId, remainingPoint] = Array.from(pointersRef.current.entries())[0];\n      panPointerRef.current = { id: remainingId, point: remainingPoint, allowed: false };\n      pinchRef.current = null;\n      return;\n    }\n    if (pointersRef.current.size === 0) {\n      panPointerRef.current = null;\n      pinchRef.current = null;\n      const enterTarget = pinchEnterIntentRef.current;\n      pinchEnterIntentRef.current = null;\n      if (enterTarget) {\n        void moveToFocus(enterTarget, 'forward');\n      } else if (pinchReturnIntentRef.current) {\n        pinchReturnIntentRef.current = false;\n        returnToParent();\n      }`);

replaceOnce('wheel child entry', `    if (\n      !editingLayout &&\n      event.deltaY > 0 &&`, `    if (!editingLayout && event.deltaY < 0 && view.scale >= MAX_SCALE - 0.01) {\n      const hit = document.elementFromPoint(event.clientX, event.clientY)?.closest<HTMLElement>('[data-network-wallet]');\n      const candidate = hit?.dataset.networkWallet;\n      if (candidate && childByWallet.has(keyWallet(candidate))) {\n        wheelEnterDistanceRef.current += Math.abs(event.deltaY);\n        if (wheelEnterDistanceRef.current >= WHEEL_ENTER_DISTANCE) {\n          wheelEnterDistanceRef.current = 0;\n          void moveToFocus(candidate, 'forward');\n        }\n        return;\n      }\n    } else if (event.deltaY >= 0 || view.scale < MAX_SCALE - 0.01) {\n      wheelEnterDistanceRef.current = 0;\n    }\n\n    if (\n      !editingLayout &&\n      event.deltaY > 0 &&`);

replaceOnce('node hold binding', `                  onPointerDown={editingLayout ? (event) => beginWorkspaceDrag(event, 'node', childKey, { x: child.x, y: child.y }) : undefined}\n                  onClick={() => {`, `                  onPointerDown={editingLayout\n                    ? (event) => beginWorkspaceDrag(event, 'node', childKey, { x: child.x, y: child.y })\n                    : (event) => beginHoldNode(event, childKey, { x: child.x, y: child.y })}\n                  onClick={() => {`);

replaceOnce('node wallet hit target', `                  data-no-pan="true"\n                  data-workspace-draggable={editingLayout ? 'true' : undefined}`, `                  data-no-pan="true"\n                  data-network-wallet={child.wallet}\n                  data-workspace-draggable={editingLayout ? 'true' : undefined}`);

replaceOnce('stage context menu guard', `        onPointerCancelCapture={onPointerEndCapture}\n        onWheel={onWheel}`, `        onPointerCancelCapture={onPointerEndCapture}\n        onContextMenu={(event) => event.preventDefault()}\n        onWheel={onWheel}`);

replaceOnce('view controls', `        <div className="viewControls" data-no-pan="true">\n          <button type="button" onClick={centerNetwork} aria-label={c.centerNetwork} title={c.centerNetwork}>◎</button>\n          <button type="button" onClick={() => zoomByButton(1)} aria-label={c.zoomIn} title={c.zoomIn}>+</button>\n          <button type="button" onClick={() => zoomByButton(-1)} aria-label={c.zoomOut} title={c.zoomOut}>−</button>\n        </div>`, `        <div className="viewControls" data-no-pan="true">\n          <button type="button" onClick={returnToRoot} aria-label={c.you} title={c.you}>◎</button>\n          <button type="button" className="fitButton" onClick={fitNetwork} aria-label={w.fit} title={w.fit}>{w.fit}</button>\n          <button type="button" onClick={() => zoomByButton(1)} aria-label={c.zoomIn} title={c.zoomIn}>+</button>\n          <button type="button" onClick={() => zoomByButton(-1)} aria-label={c.zoomOut} title={c.zoomOut}>−</button>\n        </div>`);

replaceOnce('view control css', `.viewControls{position:absolute;z-index:55;right:10px;bottom:10px;display:grid;grid-template-columns:repeat(3,34px);gap:5px}`, `.viewControls{position:absolute;z-index:55;right:10px;bottom:10px;display:grid;grid-template-columns:34px 44px 34px 34px;gap:5px}`);
replaceOnce('fit button css', `.viewControls button:hover,.pager button:hover:not(:disabled){border-color:rgba(244,183,40,.28);color:#e4c36d}`, `.viewControls button:hover,.pager button:hover:not(:disabled){border-color:rgba(244,183,40,.28);color:#e4c36d}.viewControls .fitButton{font-size:.48rem}`);
replaceOnce('touch callout css', `cursor:grab;user-select:none;-webkit-user-select:none}`, `cursor:grab;user-select:none;-webkit-user-select:none;-webkit-touch-callout:none}`);

if (!tests.includes("normal node movement requires a 500ms hold")) {
  tests += `\n\ntest('normal node movement requires a 500ms hold and never gives the hold gesture camera ownership', () => {\n  assert.match(networkSource, /const HOLD_TO_MOVE_MS = 500/);\n  assert.match(networkSource, /const beginHoldNode = useCallback/);\n  assert.match(networkSource, /hold\\.armed = true/);\n  assert.match(networkSource, /withNodePosition\\(workspaceForFocus\\(current, currentFocusKey\\), hold\\.key, nextPoint\\)/);\n  assert.match(networkSource, /if \\(hold\\.moved && currentFocusKey\\)/);\n  assert.match(networkSource, /onContextMenu=\\{\\(event\\) => event\\.preventDefault\\(\\)\\}/);\n  assert.match(networkSource, /-webkit-touch-callout:none/);\n});\n\ntest('YOU return and Fit are separate explicit controls and ancestor jumps do not reuse the wrong parent camera', () => {\n  assert.match(networkSource, /const fitNetwork = useCallback/);\n  assert.match(networkSource, /const returnToRoot = useCallback/);\n  assert.match(networkSource, /onClick=\\{returnToRoot\\}/);\n  assert.match(networkSource, /onClick=\\{fitNetwork\\}/);\n  assert.match(networkSource, /const isImmediateParent = Boolean/);\n  assert.match(networkSource, /isImmediateParent \\? returnViewByChildRef\\.current\\.get\\(current\\) : undefined/);\n});\n\ntest('extra node-centered zoom enters a child while blank-canvas zoom remains canvas-only', () => {\n  assert.match(networkSource, /pinchEnterIntentRef/);\n  assert.match(networkSource, /PINCH_ENTER_OVERSHOOT/);\n  assert.match(networkSource, /document\\.elementFromPoint\\(center\\.x, center\\.y\\)/);\n  assert.match(networkSource, /data-network-wallet=\\{child\\.wallet\\}/);\n  assert.match(networkSource, /void moveToFocus\\(enterTarget, 'forward'\\)/);\n  assert.match(networkSource, /wheelEnterDistanceRef/);\n});\n`;
}

await writeFile(networkPath, source);
await writeFile(testPath, tests);
