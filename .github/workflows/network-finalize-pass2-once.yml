name: Network Finalize Pass 2 Once

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while the guarded finalizer script is syntax-reviewed."
